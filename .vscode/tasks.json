// Sass configuration
// https://code.visualstudio.com/docs/languages/css
// https://stackoverflow.com/questions/19569990/npm-install-doesnt-work-in-windows-powershell
/*  without gulp */
/*
{
    "version": "0.1.0",
    "command": "node-sass",
    "isShellCommand": true,
    "args": ["css/styles.scss", "css/styles.css"]
}

*/
/* note to self: gulp problems: try changing Path to PATH, adding NODE_PATH */
/* ... have to install gulp-cli globally */
/* ... remember gulp-cli is gulp.cmd - should be able to run anywhere, and */
/* ... any local modules should run locally ... just so long as PowerShell */
/* ... knows where to find gulp.  Just running gulp will run gulp default (task) */
{
    "version": "0.1.0",
    "command": "gulp",
    "isShellCommand": true,
    "tasks": [
        {
            "taskName": "xarta",
            "isBuildCommand": true,
            "showOutput": "always",
            "isBackground": true
        }
    ]
}
